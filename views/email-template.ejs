<!doctype html>
<html>
  <head>
  </head>
  <body>
    <h1>Your order ( <%= email %>):</h1>
    <ul>
      <% items.forEach(function (item) { -%>
        <li><%= item %></li>
      <% }); -%>
    </ul>
    <a href="http://<%= server %>/confirm?email=<%= encodeURIComponent(email) %>&items=<%= JSON.stringify(items) %>">Confirm Order</a>
  </body>
</html>